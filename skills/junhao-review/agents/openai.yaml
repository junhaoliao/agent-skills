interface:
  display_name: "Junhao Review"
  short_description: "Evidence-backed, convention-driven code review"
  default_prompt: "Use $junhao-review to review this change against its exact target and return evidence-backed, actionable findings."

policy:
  allow_implicit_invocation: false
